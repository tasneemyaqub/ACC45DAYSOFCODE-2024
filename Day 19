#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while(T--)
    {
        int A, B;
        cin >> A >> B;
        if(A*10 > B/2*10)
            cout << "FIRST\n";
        else if(A*10 < B/2*10)
            cout << "SECOND\n";
        else
            cout << "ANY\n";
    }
}
